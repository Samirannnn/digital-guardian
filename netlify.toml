[build]
  command = "npm run build"
  publish = "dist/client"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — send all routes to index.html so TanStack Router handles them
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
