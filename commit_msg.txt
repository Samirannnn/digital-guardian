fix: make blockchain API non-fatal during all asset uploads

Previously any API error (503 cold-start, timeout, network) caused the
entire upload to fail with "API error". Now uploads always succeed:

BulkUploadZone:
- Blockchain search/protect wrapped in try/catch
- On API failure: file still uploads and saves to DB as clean
- Shows amber "Saved - verify later" badge instead of red error
- Only shows red error for real failures (storage, fingerprint, DB)

scan.functions.ts (Quick Scan):
- searchPHash and protectPHash are now non-fatal
- getEnforcement is non-fatal
- Asset always saved to DB even if blockchain unreachable
- Returns _blockchainUnavailable flag for UI to show warning

index.tsx:
- Shows amber warning toast when blockchain was unavailable
- Upload never fails due to API being cold-starting
